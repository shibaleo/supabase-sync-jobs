name: Generate Embeddings

on:
  workflow_run:
    workflows: ["GitHub Contents Fetch"]
    types: [completed]
  workflow_dispatch:

jobs:
  embed:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -e .
        working-directory: packages/semanticizer

      - name: Run embedding pipeline
        run: python -m vector_embedding
        working-directory: packages/semanticizer
        env:
          DIRECT_DATABASE_URL: ${{ secrets.DIRECT_DATABASE_URL }}
          # VOYAGE_API_KEY is retrieved from Supabase Vault

      - name: Notify on failure
        if: failure()
        run: |
          echo "Embedding generation failed"
          echo "Check the logs for details"
